<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Veterinaria Oh My Dog</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-success p-2 text-dark bg-opacity-50 " >

  <!-- Barra de navegación -->
  <nav class="navbar navbar-expand-lg navbar-light bg-light bg-success p-2 text-white bg-opacity-25 border border-success p-2 mb-2" >
    <div class="container">

      <a class="navbar-brand" style="font-size: 35px;">Publicar Nuevo Paseador </a>
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">

        </li>
      </ul>
    </div>
  </nav>





  <h1></h1>
<div class="perdidos-list">
  <%= form_with model: @paseador, url: paseadores_path do |form| %>
    
    <div class="form-group">
      <%= form.label :nombre %>
      <%= form.text_field :nombre, class: "input-tamanocampo" %>
    </div>

    <div class="form-group">
      <%= form.label :apellido %>
      <%= form.text_field :apellido, class: "input-tamanocampo" %>
    </div>

    <div class="form-group">
      <%= form.label :horarios %>
      <%= form.text_field :horarios, class: "input-tamanocampo" %>
    </div>

    <div class="form-group">
      <%= form.label :ubicacion %>
      <%= form.text_field :ubicacion, class: "input-tamanocampo" %>
    </div>
    <br>
    <div class="form-group">
      <%= form.submit "Confirmar", class: "btn-publicar btn-primary" %>
    </div>
  <% end %>

  <% if defined?(@error_message) && @error_message.present? %>
    <div class="alert alert-danger">
      <%= @error_message %>
    </div>
  <% end %>
