<p style="color: green"><%= notice %></p>
<h1>Adopcions</h1>

<div id="adopcions">
  <% @adopcions.each do |adopcion| %>
    <%= render adopcion %>
    <p>
      <%= link_to "Show this adopcion", adopcion %>
    </p>
  <% end %>
</div>

<%= link_to "New adopcion", new_adopcion_path %>
